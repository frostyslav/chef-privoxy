# Generated by Chef - local changes will be overwritten

user-manual <%= node['privoxy']['doc_dir'] %>/doc/privoxy/user-manual
confdir <%= node['privoxy']['config_dir'] %>
logdir <%= node['privoxy']['log_dir'] %>
actionsfile match-all.action # Actions that are applied to all sites and maybe overruled later on.
actionsfile default.action   # Main actions file
<% if node['privoxy']['adblockplus_subscription'] == true -%>
  <% node['privoxy']['url']['list'].each do |list| -%>
actionsfile <%= list.split("/")[-1].split(".")[0] %>.script.action
filterfile <%= list.split("/")[-1].split(".")[0] %>.script.filter
  <% end -%>
<% end -%>
actionsfile user.action      # User customizations
filterfile default.filter
filterfile user.filter      # User customizations
logfile logfile
listen-address  localhost:<%= node['privoxy']['port'] %>
toggle  <%= node['privoxy']['toggle'] %>
buffer-limit <%= node['privoxy']['buffer_limit'] %>
keep-alive-timeout <%= node['privoxy']['keepalive_timeout'] %>
socket-timeout <%= node['privoxy']['socket_timeout'] %>
<% node['privoxy']['additional_options'].each do |param, value| -%>
<%= param %> <%= value %>
<% end -%>
